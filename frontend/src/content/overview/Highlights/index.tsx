import { useTranslation } from 'react-i18next';

function Highlights({ hidePricing }: { hidePricing?: boolean }) {
  // Component content completely removed - shows nothing
  return null;
}

export default Highlights;